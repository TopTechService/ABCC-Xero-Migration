//
// Generated by JSON2Apex http://json2apex.herokuapp.com/
//

public class EwayRefundRequest {
	public static void consumeObject(JSONParser parser) {
		Integer depth = 0;
		do {
			JSONToken curr = parser.getCurrentToken();
			if (curr == JSONToken.START_OBJECT || 
				curr == JSONToken.START_ARRAY) {
				depth++;
			} else if (curr == JSONToken.END_OBJECT ||
				curr == JSONToken.END_ARRAY) {
				depth--;
			}
		} while (depth > 0 && parser.nextToken() != null);
	}

	public class Refund {
		public Integer TotalAmount {get;set;} 
		public String CurrencyCode {get;set;} 

        public Refund()
        {
            
        }
		public Refund(JSONParser parser) {
			while (parser.nextToken() != JSONToken.END_OBJECT) {
				if (parser.getCurrentToken() == JSONToken.FIELD_NAME) {
					String text = parser.getText();
					if (parser.nextToken() != JSONToken.VALUE_NULL) {
						if (text == 'TotalAmount') {
							TotalAmount = parser.getIntegerValue();
						} else if (text == 'CurrencyCode') {
							CurrencyCode = parser.getText();
						} else {
							System.debug(LoggingLevel.WARN, 'Refund consuming unrecognized property: '+text);
							consumeObject(parser);
						}
					}
				}
			}
		}
	}
	
		public Refund Refund {get;set;} 

    public EwayRefundRequest()
    {
        
    }
		public EwayRefundRequest(JSONParser parser) {
			while (parser.nextToken() != JSONToken.END_OBJECT) {
				if (parser.getCurrentToken() == JSONToken.FIELD_NAME) {
					String text = parser.getText();
					if (parser.nextToken() != JSONToken.VALUE_NULL) {
						if (text == 'Refund') {
							Refund = new Refund(parser);
						} else {
							System.debug(LoggingLevel.WARN, 'EwayRefundRequest consuming unrecognized property: '+text);
							consumeObject(parser);
						}
					}
				}
			}
		}
	
	
	public static EwayRefundRequest parse(String json) {
		return new EwayRefundRequest(System.JSON.createParser(json));
	}
}